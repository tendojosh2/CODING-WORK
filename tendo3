def find_largest(numbers):
    largest = numbers[0]
    for num in numbers:
        if num > largest:
            largest = num
    return largest

# Example usage
numbers = [3, 5, 7, 2, 8]
largest_number = find_largest(numbers)
print("Largest number:", largest_number)
